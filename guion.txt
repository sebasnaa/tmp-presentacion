######### Agregar software que ay traen lso agentes por defecto 

# Ejemplo 1     Push y Issues

    ## Mostrar como se activa tras el push, crear un issue
    ## Mostrar el gh y como podemos activar o desactivar los workflow
    ## Desactivo el basado en push y modifico el issue para que entre con
    ''' 
    issues:
        types: [edited, deleted]
    workflow_dispatch: 
    '''

# Ejemplo 2 need y condicional

    # Run the workflow. All jobs should run in parallel. 
    
    #we want to execute Job1 => Job2, Job3 => Job4

    #We also want to use this to show conditional statements 
    #Uncomment/explain the if statement on Job 3, and run the job, setting the parameter to false

    #### it will skip job 3, but notice how job 4 also skipped

    


# Ejemplo 3 variable y contexto

    # Mencionar los Contextos ver enlace WEB

    Need : Contains the outputs of all jobs that are defined as a dependency of the current job.
    env: Contains variables set in a workflow, job, or step.
   
    # environment variables
    ### you can define env variables for all jobs, specific jobs, or specific steps
    ### we've defined three workflow level env variables

    # Mencionar que You can have output variables at both the job and the step level.

    # Breve explicacion de el flujo
    ### job2 shows how you can output step variables, and job variables
    ### job3 consults the output from job 2
    ### go back to the workflow run and look at job2 and job3 output

 

    # Mencionar que esto esta obsoleto aunque hay ejemplos con este formato
        ################ Deprecated ################
        # run: echo "::set-output name=step1value::hello"

    # Comentar resultado final de los outputs de las variables

    # Var3 has step value, var2 has job value, and job1 has workflow value.
    # VAr3 tiene el valor del step
    # Var2 tiene el valor del job
    # VAr1 tiene el valor del workflow



# Ejemplo 4 Cache

    # Comentar el with que son los parametros para los uses: actions/.....

    # Ver el ejemplo de ver como hace caching de las dependencias

    # Tras comprobar como utiliza lo existente
    # npm outdated
    # npm install <packagename>@latest
    # npm install eslint@8.41.0
    # npm install eslint@8.40.0
    # // "eslint": "^8.23.0",

    # Comprobamos como ha creado un nuevo objeto en Managent caches 


# Secretos , environment y aprovals

# Ejemplo 5 Cambiamos de repo a tmp-presentacion-db

Mostramos la declaracion de env en script  database.js, usando environment y el scope entre 


# Ejemplo 6 matrix

# Por defecto se deja como está
# Podemos probar el continue-on-error: true agregando la version 14 a node
# Lo lanzamos primero agregandoe l 14 sin agregar el continue y despues lo agregamos

# Ejemplo 7


 # Agregamos un fallo al codigo para que falle Test en 7-Execution-Flow